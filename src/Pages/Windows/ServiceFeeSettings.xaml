<Window x:Class="Restaurants.Pages.Windows.ServiceFeeSettings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sozlamalar" Height="280" Width="450"
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None" 
        ResizeMode="NoResize"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        Background="#AAF0F0F0"
        BorderBrush="#555555"
        BorderThickness="1">
    <Window.Resources>
        <DropShadowEffect x:Key="CardShadow" BlurRadius="8" ShadowDepth="1" Opacity="0.15" Color="#202020"/>
        
        <!-- TextBox Style -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                                <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#B0BEC5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#B0BEC5"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border Background="#FFFFFF" CornerRadius="0" Effect="{StaticResource CardShadow}">
        <Grid>
            <!-- Header -->
            <Grid Height="40" VerticalAlignment="Top" Background="#2196F3">
                <TextBlock Text="Xizmat haqini sozlash" FontSize="16" FontWeight="SemiBold" Foreground="White" 
                           VerticalAlignment="Center" Margin="15,0,0,0"/>
                <Button Content="X" Width="30" Height="30" HorizontalAlignment="Right" Margin="0,0,10,0"
                        Background="#F44336" Foreground="White" FontWeight="Bold" Click="CloseButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="15" Effect="{StaticResource CardShadow}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#D32F2F"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#B71C1C"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
            
            <!-- Content -->
            <StackPanel Margin="25,60,25,25">
                <TextBlock Text="Xizmat haqi foizi (%)" FontSize="14" FontWeight="SemiBold" Foreground="#3F3F3F" Margin="0,0,0,10"/>
                <Border Background="#F8F9FA" CornerRadius="8" Padding="0" BorderThickness="1" BorderBrush="#EEEEEE">
                    <TextBox x:Name="txtServiceFeePercentage" Height="40" Style="{StaticResource ModernTextBoxStyle}" 
                             Margin="0" Text="10" PreviewTextInput="NumberValidationTextBox"/>
                </Border>
                
                <TextBlock Text="Bu qiymat barcha buyurtmalar uchun xizmat haqi foizini belgilaydi." 
                           FontSize="12" Foreground="#757575" TextWrapping="Wrap" Margin="0,15,0,30"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Content="Bekor qilish" Grid.Column="0" Margin="0,0,5,0" Height="45"
                            Background="#E0E0E0" Foreground="#333333" Style="{StaticResource ActionButtonStyle}"
                            Click="CancelButton_Click"/>
                    
                    <Button Content="Saqlash" Grid.Column="1" Margin="5,0,0,0" Height="45"
                            Background="#4CAF50" Style="{StaticResource ActionButtonStyle}"
                            Click="SaveButton_Click"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</Window> 