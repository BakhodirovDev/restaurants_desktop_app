<Window x:Class="Restaurants.Pages.DiscountSettings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chegirma sozlamalari" 
        Width="380" 
        Height="330"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White"
        WindowStyle="None">

    <Window.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#B0BEC5"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SimpleTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#BBDEFB"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2196F3"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="UnitLabelStyle" TargetType="Border">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Width" Value="50"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Border BorderThickness="1" BorderBrush="#DDDDDD">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#2196F3" Padding="15,12">
                <TextBlock Text="Chegirma sozlamalari" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="15">
                <TextBlock Text="Chegirma turi:" FontSize="15" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,12"/>

                <!-- Percentage discount option -->
                <RadioButton x:Name="rbPercentage" Content="Foiz bo'yicha (%)" GroupName="DiscountType" 
                             Margin="0,5,0,8" IsChecked="True" Checked="DiscountType_Checked"
                             FontSize="14" Foreground="#333333"/>

                <Grid Margin="0,0,0,12" IsEnabled="{Binding IsChecked, ElementName=rbPercentage}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtDiscountPercentage" PreviewTextInput="NumericTextBox_PreviewTextInput"
                             Style="{StaticResource SimpleTextBoxStyle}" 
                             GotFocus="PercentageTextBox_GotFocus"/>
                    <Border Grid.Column="1" Style="{StaticResource UnitLabelStyle}" Margin="8,0,0,0">
                        <TextBlock Text="%" FontSize="14" FontWeight="SemiBold" Foreground="#333333" 
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                </Grid>

                <!-- Amount discount option -->
                <RadioButton x:Name="rbAmount" Content="Summa bo'yicha" GroupName="DiscountType" 
                             Margin="0,5,0,8" Checked="DiscountType_Checked"
                             FontSize="14" Foreground="#333333"/>

                <Grid Margin="0,0,0,15" IsEnabled="{Binding IsChecked, ElementName=rbAmount}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtDiscountAmount" PreviewTextInput="NumericTextBox_PreviewTextInput"
                             Style="{StaticResource SimpleTextBoxStyle}"
                             GotFocus="AmountTextBox_GotFocus"/>
                    <Border Grid.Column="1" Style="{StaticResource UnitLabelStyle}" Margin="8,0,0,0">
                        <TextBlock Text="so'm" FontSize="14" FontWeight="SemiBold" Foreground="#333333" 
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                </Grid>

                <!-- Discount preview section - hidden but needed for code-behind -->
                <TextBlock x:Name="lblDiscountPreview" Visibility="Collapsed"/>

                <!-- Discount preview section removed as it doesn't appear in the image -->
            </StackPanel>

            <!-- Footer with buttons -->
            <Border Grid.Row="2" Background="White" Padding="15,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="btnResetDiscount" Content="Bekor qilish" Grid.Column="0" Height="40" 
                            Background="#F44336" Style="{StaticResource ActionButtonStyle}"
                            Click="btnResetDiscount_Click" Margin="0,0,5,0"/>

                    <Button x:Name="btnSave" Content="Saqlash" Grid.Column="1" Height="40" 
                            Background="#4CAF50" Style="{StaticResource ActionButtonStyle}"
                            Click="btnSave_Click" Margin="5,0,0,0"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window> 